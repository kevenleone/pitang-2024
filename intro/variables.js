let number = -1.23;

const bigInt = BigInt(10);

number += 100;
number -= 100;
number /= 100;
number *= 100;

const loading = true;
const test1 = null;
const test = undefined;
const name = 'Fulano falou: "blablabla" ';

const user = {
  name: 'Keven Leone',
  age: 28,
  city: ['Recife', 'Palmares'],
  school: [
    {
      name: 'POLI',
      course: 'Analytics...',
    },
    {
      name: 'FAFICA',
      course: 'Analytics...',
    },
    {
      name: 'UNICAP',
      course: 'Analytics...',
    },
  ],
};

delete user.name;
delete user.age;

const users = [user, user, user, user];

const school = {};

'keven'.replaceAll('e', 'f'); // kfven

polyfills;

school.name = 'Keven';

// Infinity - Infinity;
// NaN;
